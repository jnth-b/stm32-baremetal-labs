# Projects Index

This folder keeps each STM32 experiment isolated so updates stay organized.

- `10_adc_continuous_conversion` - ADC1 continuous sampling on `PA1`, prints values over UART2
- `11_systick_delay` - SysTick-based millisecond delay with LED toggle and UART logs
